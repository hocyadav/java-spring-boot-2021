```
different name same meaning
map == sync / hardcoded logic / fun() return type /  hardcoded fallback / sync_pipeline like stream / sync_fallback like stream
flatmap == async / pipeline / fallback / async_pipeline / async_fallback

onError Return == sync / --""--
onError Resume == asnyc / --""--

default IfEmpty == sync / --""--
switch IfEmpty == async / --""--

```